server:
  port: 8702
eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8699/eureka
  instance:
    hostname: localhost

#指定应用名称
spring:
  application:
    name: eureka-service
